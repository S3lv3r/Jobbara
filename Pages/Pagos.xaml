<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Jobbara.Pages.Pagos"
             BackgroundColor="White"
              xmlns:shell="http://schemas.microsoft.com/dotnet/2021/maui"
shell:Shell.NavBarIsVisible="False"
             
             Title="Pagos">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Fondo decorativo superior -->
        <Grid Grid.Row="0">
            <BoxView HeightRequest="200"
                     BackgroundColor="#FF9B51"
                     CornerRadius="0,0,200,200"
                     HorizontalOptions="Fill"
                     VerticalOptions="Start" />

            <!-- Botón de regreso -->
            <ImageButton Source="back.png"
                         HorizontalOptions="Start"
                         VerticalOptions="Start"
                         Clicked="OnGoToHome"
                         Margin="10"
                         WidthRequest="28"
                         HeightRequest="28"
                         BackgroundColor="#FF9B51"/>

            <ScrollView Margin="0,50,0,0">
                <VerticalStackLayout Padding="20" Spacing="15">

                    <Label Text="Trabajos Concluidos"
                           FontSize="28"
                           FontAttributes="Bold"
                           TextColor="White"
                           HorizontalOptions="Center"
                           Margin="0,10,0,0"/>

                    <!-- Lista de trabajos concluidos -->
                    <CollectionView x:Name="TrabajosCollection">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Frame Margin="0,5"
                                       Padding="15"
                                       CornerRadius="10"
                                       BackgroundColor="#E8F5E9"
                                       BorderColor="#388E3C"
                                       HasShadow="True">
                                    <VerticalStackLayout>
                                        <Label Text="{Binding Titulo}"
                                               FontAttributes="Bold"
                                               FontSize="18"
                                               TextColor="#2E7D32"/>
                                        <Label Text="{Binding Fecha}"
                                               FontSize="12"
                                               TextColor="Gray"/>
                                        <Label Text="Estado: Completado"
                                               FontSize="13"
                                               TextColor="#1B5E20"/>
                                        <Label Text="{Binding Pago}"
                                               FontSize="14"
                                               TextColor="Black"
                                               FontAttributes="Bold"/>
                                    </VerticalStackLayout>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>

                </VerticalStackLayout>
            </ScrollView>
        </Grid>

        <!-- Barra inferior -->
        <Grid Grid.Row="1"
              BackgroundColor="#F0F0F0"
              Padding="10"
              HeightRequest="70"
              ColumnSpacing="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Ajustes -->
            <ImageButton Source="settings.png"
                         BackgroundColor="Transparent"
                         Grid.Column="0"
                         HorizontalOptions="Center"
                         VerticalOptions="Center"
                         Clicked="GoToAjustes"
                         WidthRequest="40"
                         Margin="0,-20,0,0"/>
            <Label Text="Ajustes" TextColor="DarkOrange"
                   FontAttributes="Bold"
                   Grid.Column="0"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"
                   Margin="0,34,0,0"
                   FontSize="12"/>

            <!-- Botón central -->
            <Frame HeightRequest="80"
                   WidthRequest="80"
                   CornerRadius="40"
                   BackgroundColor="#FF9B51"
                   Padding="0"
                   HasShadow="True"
                   BorderColor="OrangeRed"
                   VerticalOptions="Center"
                   HorizontalOptions="Center"
                   Grid.Column="1"
                   Margin="0,-30,0,0">
                <ImageButton Source="plus.png"
                             BackgroundColor="Transparent"
                             Aspect="AspectFit"
                             Padding="0"/>
            </Frame>

            <!-- Pagos -->
            <ImageButton Source="money.png"
                         BackgroundColor="Transparent"
                         Grid.Column="2"
                         HorizontalOptions="Center"
                         VerticalOptions="Center"
                         Margin="0,-16,0,0"
                         Clicked="GoToPagos"
                         WidthRequest="47"/>
            <Label Text="Pagos" TextColor="DarkOrange"
                   FontAttributes="Bold"
                   Grid.Column="2"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"
                   Margin="0,34,0,0"
                   FontSize="12"/>
        </Grid>
    </Grid>
</ContentPage>