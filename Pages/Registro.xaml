<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Jobbara.Pages.Registro"
             Title="Registro"
             xmlns:shell="http://schemas.microsoft.com/dotnet/2021/maui"
             shell:Shell.NavBarIsVisible="False"
             BackgroundColor="White">

<<<<<<< HEAD
    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Start">

            <!-- icono-->
            <Image 
                Source="app_icon.png" 
                HorizontalOptions="Center" 
                HeightRequest="60" 
                WidthRequest="60" />

            <!-- sign up-->
            <Label 
                Text="Sign up to Jobbara" 
                FontSize="24" 
                FontAttributes="Bold" 
                HorizontalOptions="Center" 
                TextColor="Black" />

            <!-- dqatos-->
            <BoxView HeightRequest="20" Color="Transparent" />

            <!-- Campos de texto -->
            <Entry Placeholder="Name" Keyboard="Default" TextColor="Black" x:Name="userNameTxt" />
            <Entry Placeholder="Email Address" Keyboard="Email" TextColor="Black" x:Name="emailTxt" />
            <Entry Placeholder="Password" IsPassword="True" TextColor="Black" x:Name="passwordTxt"/>

            <!-- sign up btn -->
            <Button 
                Text="Sign Up" 
                BackgroundColor="#FF9B51" 
                TextColor="White" 
                CornerRadius="20"
                HeightRequest="50"
                FontAttributes="Bold"
                Clicked="RegisterUserDB_Cliked"/>

            <HorizontalStackLayout VerticalOptions="Center" Spacing="10" HorizontalOptions="Center">
                <Label Text="or" TextColor="#999999" FontAttributes="Bold" />
            </HorizontalStackLayout>

            <!-- sig up google -->
            <Button 
                Text="Sign up with Google" 
                BackgroundColor="White" 
                TextColor="Black"
                BorderColor="#CCCCCC"
                BorderWidth="1"
                CornerRadius="20"
                HeightRequest="50"
                FontAttributes="Bold" 
                />

            <!-- espacio -->
            <BoxView HeightRequest="20" Color="Transparent" />

            <!-- por si ya tienes cuenta -->
            <Label 
            Text="Do you already have an account?" 
            TextColor="#007AFF" 
            HorizontalOptions="Center"
            FontAttributes="Italic">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer
                    Tapped="OnLoginTapped"
                    NumberOfTapsRequired="1" />
                </Label.GestureRecognizers>
            </Label>

        </VerticalStackLayout>
    </ScrollView>
=======
    <Grid>
        <!-- Fondo con degradado -->


        <ScrollView>
            <VerticalStackLayout Padding="30" Spacing="25">

                <!-- icono-->
                <Image Source="app_icon.png"
                       HorizontalOptions="Center"
                       HeightRequest="80"
                       WidthRequest="80" />

                <!-- sign up título-->
                <Label Text="Sign up to Jobbara"
                       FontSize="26"
                       FontAttributes="Bold"
                       HorizontalOptions="Center"
                       TextColor="#333333" />

                <!-- espacio -->
                <BoxView HeightRequest="20" Color="Transparent" />

                <!-- Campos de texto con diseño mejorado -->
                <Frame CornerRadius="12" Padding="5" BackgroundColor="#f4f4f5">
                    <Entry Placeholder="Name"
                           Keyboard="Default"
                           TextColor="Black"
                           x:Name="userNameTxt"
                           BackgroundColor="#F5F5F5" />
                </Frame>

                <Frame CornerRadius="12" Padding="5" BackgroundColor="#f4f4f5">
                    <Entry Placeholder="Email Address"
                           Keyboard="Email"
                           TextColor="Black"
                           x:Name="emailTxt"
                           BackgroundColor="#F5F5F5" />
                </Frame>

                <Frame CornerRadius="12" Padding="5" BackgroundColor="#f4f4f5">
                    <Entry Placeholder="Password"
                           IsPassword="True"
                           TextColor="Black"
                           x:Name="passwordTxt"
                           BackgroundColor="#F5F5F5" />
                </Frame>

                <!-- sign up btn -->
                <Button Text="Sign Up"
                        BackgroundColor="#FF7043"
                        TextColor="White"
                        CornerRadius="25"
                        HeightRequest="55"
                        FontAttributes="Bold"
                        Clicked="RegisterUserDB_Cliked"
                        Padding="10" />

                <HorizontalStackLayout Spacing="10" HorizontalOptions="Center">
                    <Label Text="or" TextColor="#999999" FontAttributes="Bold" />
                </HorizontalStackLayout>

                <!-- iniciar con google -->
                <Button Text="Sign up with Google"
                        BackgroundColor="White"
                        TextColor="Black"
                        BorderColor="#CCCCCC"
                        BorderWidth="1"
                        CornerRadius="25"
                        HeightRequest="55"
                        FontAttributes="Bold" />

                <!-- espacio extra -->
                <BoxView HeightRequest="20" Color="Transparent" />

                <!-- por si ya tienes cuenta -->
                <Label Text="Do you already have an account? Log in here!"
                       TextColor="#007AFF"
                       HorizontalOptions="Center"
                       FontAttributes="Italic">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnLoginTapped" NumberOfTapsRequired="1" />
                    </Label.GestureRecognizers>
                </Label>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
>>>>>>> 50b5b32 (Primer commit bro)
</ContentPage>
