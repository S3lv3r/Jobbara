<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Jobbara.Pages.EditarPerfil"
             Title="EditarPerfil"
             BackgroundColor="White"
             xmlns:shell="http://schemas.microsoft.com/dotnet/2021/maui"
             shell:Shell.NavBarIsVisible="False">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <Label Text="Conviértete en Chambeador" FontSize="26" FontAttributes="Bold" HorizontalOptions="Center" TextColor="Black"/>

            <!-- Campos -->
            <Entry Placeholder="Usuario" x:Name="UsuarioEntry" TextColor="Black"/>
            <Entry Placeholder="Email" x:Name="emailEntry" TextColor="Black" />

            <!-- Imagen seleccionada -->
            <Image x:Name="SelectedImage"
                   WidthRequest="150"
                   HeightRequest="150"
                   Aspect="AspectFill"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"
                   BackgroundColor="#eee"
                   />

            <Button Text="Seleccionar Imagen"
                    Clicked="OnSelectImageClicked"
                    BackgroundColor="#FF7043"
                    TextColor="White"/>

            <!-- Botón para guardar -->
            <Button Text="Guardar datos"
                    BackgroundColor="#FF7043"
                    TextColor="White"
                    Clicked="OnSaveClicked"
                    Margin="0,20,0,0"/>

            <!-- Botón de Confirmar y volver al perfil -->
            <Button Text="Confirmar y volver al perfil"
                    BackgroundColor="#FF9B51"
                    TextColor="White"
                    Clicked="OnConfirmClicked"
                    Margin="0,10,0,0"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
